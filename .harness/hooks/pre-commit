#!/usr/bin/env bash
# .harness/hooks/pre-commit
#
# Installed into .git/hooks/pre-commit by:
#   .harness/platform-adapters/opencode/setup.sh
#
# Runs harness invariant checks before every commit.
# Exit non-zero aborts the commit.

set -euo pipefail

REPO_ROOT="$(git rev-parse --show-toplevel)"

bash "${REPO_ROOT}/.harness/scripts/check-skill-symlinks.sh"
